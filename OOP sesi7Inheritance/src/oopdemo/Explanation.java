package oopdemo;
public class Explanation {
    public static void main(String[] args) {

        // Inheritance, allows to derive new clasees from existing classes
        // Used to avoid code duplication
        // process where one object acquires the properties of another with the most commonly used keyword "extends" or "implements"
        // ditandain dengan segitiga, merujuk ke class induknya
        // A class only can extends 1 other class (in java). In Python, a class can inherits more than 1 classes.
        // semua yg public dan protected akan diwariskan.

        //superclass

        //final Class
        //Class that couldn't be extends
        //final method can't be override by child classes
        //semacam protection agar tidak bisa diubah2

        //static method
        //nilai akan menjadi sama untuk setiap object.
        //static sudah pasti final


        System.out.println("Hello, World!");
    }
}
